/**
 * sum_them_all - sum arguments
 * @array: array
 * 
 * 
 *
 * Return: integer
 */

int sum_them_all(const unsigned int n, ...)
{
	if (array && cmp)
	{
		int i;

		if (size <= 0)
			return (-1);

		for (i = 0; i < size; i++)
		{
			if (cmp(array[i]))
				return (i);
		}
	}
	return (-1);
}
