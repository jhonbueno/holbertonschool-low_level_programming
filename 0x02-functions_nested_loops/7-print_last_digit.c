#include "main.h"

/**
 * _print_last_digit - Print last digit
 * @n: parameter n
 *
 * Return: last digit
 */

int _print_last_digit(int n)
{
	int last_digit;

	last_digit = n % 10;

	return (last_digit);
}
